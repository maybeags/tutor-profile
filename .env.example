# Google Apps Script Web App URL that appends submitted profiles to a Google Sheet.
# See docs/google-sheet-setup.md for how to create it.
# Leave empty to disable sheet logging (form still works, just doesn't log anywhere).
VITE_SHEET_WEBHOOK_URL=
